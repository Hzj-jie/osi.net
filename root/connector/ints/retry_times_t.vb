
Option Explicit On
Option Infer Off
Option Strict On

'the following code is generated by /osi/root/codegen/precompile/precompile.exe
'with retry_times_t.vbp ----------
'so change retry_times_t.vbp instead of this file



'the following code is generated by /osi/root/codegen/precompile/precompile.exe
'with npos_uint.vbp ----------
'so change npos_uint.vbp instead of this file



Imports System.IO
Imports osi.root.constants
Imports constants = osi.root.constants

Partial Public Structure retry_times_t
    Implements IComparable, IComparable(Of retry_times_t), ICloneable, ICloneable(Of retry_times_t)

    Public Shared ReadOnly inf As retry_times_t =
        Function() As retry_times_t
            Dim inf As retry_times_t = New retry_times_t(constants.npos)
            assert(inf.npos())
            Return inf
        End Function()
    Public Shared ReadOnly zero As retry_times_t =
        Function() As retry_times_t
            Dim zero As retry_times_t = New retry_times_t(uint32_0)
            If True Then
                assert(Not zero.npos())
            Else
                assert(zero.npos())
            End If
            Return zero
        End Function()
    Public Shared ReadOnly sizeof_value As UInt32 = CUInt(sizeof(Of UInt32)())

    <Diagnostics.CodeAnalysis.SuppressMessage("Microsoft.Usage", "CA2207:InitializeValueTypeStaticFieldsInline")>
    Shared Sub New()
        assert(constants.npos < 0)
        Dim x As retry_times_t = Nothing
        assert(Not x.npos())
        assert(x.raw_value() = uint32_0)

        bytes_serializer.fixed.register(
                Function(ByVal i As retry_times_t, ByVal o As MemoryStream) As Boolean
                    #If Not True Then
                        If i.npos() Then
                            Return bytes_serializer.append_to(uint32_0, o)
                        End If
                        Return bytes_serializer.append_to(i.raw_value(), o)
                    #ElseIf Not False Then
                        If i.npos() Then
                            Return bytes_serializer.append_to(max_uint32, o)
                        End If
                        Return bytes_serializer.append_to(i.raw_value(), o)
                    #Else
                        If i.npos() Then
                            Return bytes_serializer.append_to(True, o)
                        End If
                        Return bytes_serializer.append_to(False, o) AndAlso
                               bytes_serializer.append_to(i.raw_value(), o)
                    #End If
                End Function,
                Function(ByVal i As MemoryStream, ByRef o As retry_times_t) As Boolean
                    #If Not True OrElse Not False Then
                        Dim u As UInt32 = uint32_0
                        If Not bytes_serializer.consume_from(i, u) Then
                            Return False
                        End If
                        o = New retry_times_t(u)
                        Return True
                    #Else
                        Dim n As Boolean = False
                        If Not bytes_serializer.consume_from(i, n) Then
                            Return False
                        End If
                        If n Then
                            o = inf
                            Return True
                        End If
                        Dim u As UInt32 = uint32_0
                        If Not bytes_serializer.consume_from(i, u) Then
                            Return False
                        End If
                        o = New retry_times_t(u)
                        Return True
                    #End If
                End Function)
    End Sub

    Private ReadOnly i As UInt32
    Private ReadOnly n As Boolean

#If "Int32" <> "SByte" Then

'the following code is generated by /osi/root/codegen/precompile/precompile.exe
'with npos_uint_ctor.vbp ----------
'so change npos_uint_ctor.vbp instead of this file



    Public Sub New(ByVal i As SByte)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "SByte" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If

        If i < 0 Then
            n = True
        Else
            Me.i = CUInt(i)
        End If
    End Sub

#If True Then
    Public Sub New(ByVal i As Byte)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "SByte" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If
        Me.i = CUInt(i)
    End Sub
#End If

    Public Shared Widening Operator CType(ByVal this As SByte) As retry_times_t
        Return New retry_times_t(this)
    End Operator

#If True Then
    Public Shared Widening Operator CType(ByVal this As Byte) As retry_times_t
        Return New retry_times_t(this)
    End Operator
#End If

'finish npos_uint_ctor.vbp --------
#End If

#If "Int32" <> "Int16" AndAlso "Int32" <> "Short" Then

'the following code is generated by /osi/root/codegen/precompile/precompile.exe
'with npos_uint_ctor.vbp ----------
'so change npos_uint_ctor.vbp instead of this file



    Public Sub New(ByVal i As Int16)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "Int16" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If

        If i < 0 Then
            n = True
        Else
            Me.i = CUInt(i)
        End If
    End Sub

#If True Then
    Public Sub New(ByVal i As UInt16)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "Int16" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If
        Me.i = CUInt(i)
    End Sub
#End If

    Public Shared Widening Operator CType(ByVal this As Int16) As retry_times_t
        Return New retry_times_t(this)
    End Operator

#If True Then
    Public Shared Widening Operator CType(ByVal this As UInt16) As retry_times_t
        Return New retry_times_t(this)
    End Operator
#End If

'finish npos_uint_ctor.vbp --------
#End If

#If "Int32" <> "Int32" AndAlso "Int32" <> "Integer" Then

'the following code is generated by /osi/root/codegen/precompile/precompile.exe
'with npos_uint_ctor.vbp ----------
'so change npos_uint_ctor.vbp instead of this file



    Public Sub New(ByVal i As Int32)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "Int32" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If

        If i < 0 Then
            n = True
        Else
            Me.i = CUInt(i)
        End If
    End Sub

#If True Then
    Public Sub New(ByVal i As UInt32)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "Int32" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If
        Me.i = CUInt(i)
    End Sub
#End If

    Public Shared Widening Operator CType(ByVal this As Int32) As retry_times_t
        Return New retry_times_t(this)
    End Operator

#If True Then
    Public Shared Widening Operator CType(ByVal this As UInt32) As retry_times_t
        Return New retry_times_t(this)
    End Operator
#End If

'finish npos_uint_ctor.vbp --------
#End If

#If "Int32" <> "Int64" AndAlso "Int32" <> "Long" Then

'the following code is generated by /osi/root/codegen/precompile/precompile.exe
'with npos_uint_ctor.vbp ----------
'so change npos_uint_ctor.vbp instead of this file



    Public Sub New(ByVal i As Int64)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "Int64" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If

        If i < 0 Then
            n = True
        Else
            Me.i = CUInt(i)
        End If
    End Sub

#If True Then
    Public Sub New(ByVal i As UInt64)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "Int64" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If
        Me.i = CUInt(i)
    End Sub
#End If

    Public Shared Widening Operator CType(ByVal this As Int64) As retry_times_t
        Return New retry_times_t(this)
    End Operator

#If True Then
    Public Shared Widening Operator CType(ByVal this As UInt64) As retry_times_t
        Return New retry_times_t(this)
    End Operator
#End If

'finish npos_uint_ctor.vbp --------
#End If

#If "Int32" <> "Decimal" Then

'the following code is generated by /osi/root/codegen/precompile/precompile.exe
'with npos_uint_ctor.vbp ----------
'so change npos_uint_ctor.vbp instead of this file



    Public Sub New(ByVal i As Decimal)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "Decimal" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If

        If i < 0 Then
            n = True
        Else
            Me.i = CUInt(i)
        End If
    End Sub

#If False Then
    Public Sub New(ByVal i As UDecimal)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "Decimal" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If
        Me.i = CUInt(i)
    End Sub
#End If

    Public Shared Widening Operator CType(ByVal this As Decimal) As retry_times_t
        Return New retry_times_t(this)
    End Operator

#If False Then
    Public Shared Widening Operator CType(ByVal this As UDecimal) As retry_times_t
        Return New retry_times_t(this)
    End Operator
#End If

'finish npos_uint_ctor.vbp --------
#End If


'the following code is generated by /osi/root/codegen/precompile/precompile.exe
'with npos_uint_ctor.vbp ----------
'so change npos_uint_ctor.vbp instead of this file



    Public Sub New(ByVal i As Int32)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "Int32" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If

        If i < 0 Then
            n = True
        Else
            Me.i = CUInt(i)
        End If
    End Sub

#If True Then
    Public Sub New(ByVal i As UInt32)
#If Not True Then
        If i = 0 Then
            n = True
            Return
        End If
#End If
#If "Int32" <> "Int32" Then
        If i > max_uint32 Then
            n = True
            Return
        End If
#End If
#If Not False Then
        If i = max_uint32 Then
            n = True
            Return
        End If
#End If
        Me.i = CUInt(i)
    End Sub
#End If

    Public Shared Widening Operator CType(ByVal this As Int32) As retry_times_t
        Return New retry_times_t(this)
    End Operator

#If True Then
    Public Shared Widening Operator CType(ByVal this As UInt32) As retry_times_t
        Return New retry_times_t(this)
    End Operator
#End If

'finish npos_uint_ctor.vbp --------

    Public Sub New(ByVal that As retry_times_t)
        i = that.i
        n = that.n
    End Sub

#If False Then
    Public Function value() As UInt32
#If Not DEBUG Then
        assert(Not npos())
#End If
        Return raw_value()
    End Function
#End If

    Private Function raw_value() As UInt32
#If DEBUG Then
        assert(Not npos())
#End If
        Return i
    End Function

#If False Then
    Public Function npos() As Boolean
#Else
    Private Function npos() As Boolean
#End If
        Return n
    End Function

#If True Then
    Public Function infinite() As Boolean
        Return npos()
    End Function
#End If

    Public Shared Operator =(ByVal this As retry_times_t, ByVal that As retry_times_t) As Boolean
        If this.npos() = that.npos() Then
            If this.npos() Then
                Return True
            End If
            Return this.raw_value() = that.raw_value()
        End If
        Return False
    End Operator

    Public Shared Operator <>(ByVal this As retry_times_t, ByVal that As retry_times_t) As Boolean
        Return Not (this = that)
    End Operator

    Public Shared Operator =(ByVal this As retry_times_t, ByVal that As Int32) As Boolean
        If that < 0 AndAlso this.npos() Then
            Return True
        End If
        Return this.raw_value() = CUInt(that)
    End Operator

    Public Shared Operator <>(ByVal this As retry_times_t, ByVal that As Int32) As Boolean
        Return Not (this = that)
    End Operator

    Public Shared Operator =(ByVal this As Int32, ByVal that As retry_times_t) As Boolean
        Return that = this
    End Operator

    Public Shared Operator <>(ByVal this As Int32, ByVal that As retry_times_t) As Boolean
        Return that <> this
    End Operator

    Public Shared Operator =(ByVal this As retry_times_t, ByVal that As UInt32) As Boolean
        If this.npos() Then
            Return False
        End If
        Return this.raw_value() = that
    End Operator

    Public Shared Operator <>(ByVal this As retry_times_t, ByVal that As UInt32) As Boolean
        Return Not (this = that)
    End Operator

    Public Shared Operator =(ByVal this As UInt32, ByVal that As retry_times_t) As Boolean
        Return that = this
    End Operator

    Public Shared Operator <>(ByVal this As UInt32, ByVal that As retry_times_t) As Boolean
        Return that <> this
    End Operator

    Public Shared Operator <(ByVal this As retry_times_t, ByVal that As retry_times_t) As Boolean
        If this.npos() Then
            Return False
        End If
        If that.npos() Then
            Return True
        End If
        Return this.raw_value() < that.raw_value()
    End Operator

    Public Shared Operator >(ByVal this As retry_times_t, ByVal that As retry_times_t) As Boolean
        If that.npos() Then
            Return False
        End If
        If this.npos() Then
            Return True
        End If
        Return this.raw_value() > that.raw_value()
    End Operator

    Public Shared Operator <(ByVal this As retry_times_t, ByVal that As Int32) As Boolean
        If that <= 0 OrElse
           this.npos() OrElse
           this.raw_value() >= max_int32 Then
            Return False
        End If
        Return this.raw_value() < CUInt(that)
    End Operator

    Public Shared Operator >(ByVal this As retry_times_t, ByVal that As Int32) As Boolean
        If that < 0 Then
            Return False
        End If
        If this.npos() OrElse this.raw_value() > max_int32 Then
            Return True
        End If
        Return this.raw_value() > CUInt(that)
    End Operator

    Public Shared Operator <(ByVal this As Int32, ByVal that As retry_times_t) As Boolean
        Return that > this
    End Operator

    Public Shared Operator >(ByVal this As Int32, ByVal that As retry_times_t) As Boolean
        Return that < this
    End Operator

    Public Shared Operator <(ByVal this As retry_times_t, ByVal that As UInt32) As Boolean
        If this.npos() Then
            Return False
        End If
        Return this.raw_value() < that
    End Operator

    Public Shared Operator >(ByVal this As retry_times_t, ByVal that As UInt32) As Boolean
        If this.npos() Then
            Return True
        End If
        Return this.raw_value() > that
    End Operator

    Public Shared Operator <(ByVal this As UInt32, ByVal that As retry_times_t) As Boolean
        Return that > this
    End Operator

    Public Shared Operator >(ByVal this As UInt32, ByVal that As retry_times_t) As Boolean
        Return that < this
    End Operator

    Public Shared Operator <=(ByVal this As retry_times_t, ByVal that As retry_times_t) As Boolean
        If that.npos() Then
            Return True
        End If
        If this.npos() Then
            Return False
        End If
        Return this.raw_value() <= that.raw_value()
    End Operator

    Public Shared Operator >=(ByVal this As retry_times_t, ByVal that As retry_times_t) As Boolean
        If this.npos() Then
            Return True
        End If
        If that.npos() Then
            Return False
        End If
        Return this.raw_value() >= that.raw_value()
    End Operator

    Public Shared Operator <=(ByVal this As retry_times_t, ByVal that As Int32) As Boolean
        If that < 0 OrElse
           this.npos() OrElse
           this.raw_value() > max_int32 Then
            Return False
        End If
        Return this.raw_value() <= CUInt(that)
    End Operator

    Public Shared Operator >=(ByVal this As retry_times_t, ByVal that As Int32) As Boolean
        If this.npos() Then
            Return True
        End If
        If that < 0 Then
            Return False
        End If
        If this.raw_value() >= max_int32 Then
            Return True
        End If
        Return this.raw_value() >= CUInt(that)
    End Operator

    Public Shared Operator <=(ByVal this As Int32, ByVal that As retry_times_t) As Boolean
        Return that >= this
    End Operator

    Public Shared Operator >=(ByVal this As Int32, ByVal that As retry_times_t) As Boolean
        Return that <= this
    End Operator

    Public Shared Operator <=(ByVal this As retry_times_t, ByVal that As UInt32) As Boolean
        If this.npos() Then
            Return False
        End If
        Return this.raw_value() <= that
    End Operator

    Public Shared Operator >=(ByVal this As retry_times_t, ByVal that As UInt32) As Boolean
        If this.npos() Then
            Return True
        End If
        Return this.raw_value() >= that
    End Operator

    Public Shared Operator <=(ByVal this As UInt32, ByVal that As retry_times_t) As Boolean
        Return that >= this
    End Operator

    Public Shared Operator >=(ByVal this As UInt32, ByVal that As retry_times_t) As Boolean
        Return that <= this
    End Operator

    Public Shared Widening Operator CType(ByVal this As retry_times_t) As Int32
        If this.npos() Then
            Return constants.npos
        End If
        assert(this.raw_value() <= max_int32)
        Return CInt(this.raw_value())
    End Operator

    Public Shared Widening Operator CType(ByVal this As retry_times_t) As UInt32
        assert(Not this.npos())
        Return this.raw_value()
    End Operator

#If False Then
    Public Shared Operator +(Byval this As retry_times_t) As UInt32
        Return this.raw_value()
    End Operator    
#End If

    Public Overrides Function ToString() As String
        If npos() Then
            Return Convert.ToString(constants.npos)
        End If
        Return Convert.ToString(raw_value())
    End Function

    Public Function CompareTo(ByVal that As retry_times_t) As Int32 Implements IComparable(Of retry_times_t).CompareTo
        If npos() = that.npos() Then
            Return 0
        End If
        If npos() Then
            Return 1
        End If
        If that.npos() Then
            Return -1
        End If
        Return raw_value().CompareTo(that.raw_value())
    End Function

    Public Function CompareTo(ByVal that As Object) As Int32 Implements IComparable.CompareTo
        Dim other As retry_times_t = Nothing
        If cast(Of retry_times_t)(that, other) Then
            Return CompareTo(other)
        End If
        Return CompareTo(zero)
    End Function

    Public Function CloneT() As retry_times_t Implements ICloneable(Of retry_times_t).Clone
        Return New retry_times_t(Me)
    End Function

    Public Function Clone() As Object Implements ICloneable.Clone
        Return CloneT()
    End Function
End Structure
'finish npos_uint.vbp --------

Partial Public Structure retry_times_t
    Public Function attempts() As UInt32
        assert(Not npos())
        Return raw_value() + uint32_1
    End Function

    Public Shared Operator +(ByVal this As retry_times_t) As UInt32
        Return this.attempts()
    End Operator
End Structure
'finish retry_times_t.vbp --------
