
#include <b2style.h>

#include "cstdio"

int main() {
  int x = getchar();
  putchar(x);
  x = getchar();
  putchar(x);
  x = getchar();
  putchar(x);
  x = getchar();
  if (x == EOF) {
    b2style::std_out("EoF");
  }
}
