
#include <b2style.h>

template<RT, T>
delegate RT function(T);

int inc(int i) {
  return ++i;
}

void write(string s) {
  b2style::std_out(s);
}

void main() {
  function<int, int> f = inc;
  b2style::std_out(f(1));

  function<void, string> f2 = write;
  f2("abc");
}