
#include <bstyle.h>

struct S1 {
  int x;
};

struct S2 {
  S1 x;
  int y;
};

void print(S1 s) {
  bstyle__putchar(s.x);
}

void print(S2 s) {
  print(s.x);
  bstyle__putchar(s.y);
}

void main() {
  S2 s;
  s.x.x = 100;
  s.y = 100;
  print(s);
}